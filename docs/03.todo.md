# 临时想法收集器


------------
function ChooseTool(query: string, additional_context: string) -> BashTool | ReadTool | WriteTool | EditTool | WebSearchTool | WebFetchTool | UseSkill {
  client "openai/gpt-5"
  prompt #"
    Given the user query and context, determine which tool to use.

    {{ ctx.output_format }} 

    {{ _.role('user') }}
    Query: {{ query }}
    Context: {{ additional_context }}
  "#
}

我看你新加了这个 choose two 工具。这个本身可能有问题，整个 toot 库中，choose two 应该是跟 React 循环结合在一起的。也就是说，它应该是正常聊天中的一环。很有可能是这个样子的，而不是像现在这样，你自己加一个 cursor，然后回头自己偷偷地去做调用

对另外，这里面的 client 写了一个 GPT-5，这是不对的。这里可能需要你反思，或者写到 lesson 里面去

关于 client 的内容，目前你只能写 clients 目录下的定义。比如现在有 GLM-40、7 这样的文件