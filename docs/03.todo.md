# 临时想法收集器


func (my *EngineStream) executeToolCall(result *ChatResult, ch chan<- StreamChunk) (string, string, error) {
我看这里你重写了一遍useTool方法， 把对各种工具的调用逻辑在stream中重写了一遍，我想知道， 我想知道在整个项目中，你到底使用的是这个 engine stream，还是原来 engine 里的那个 tool call，也就是 use tool 的方法

如果只是使用其中一个而另一个用不到的话，我们是不是就不需要写另外一个了？因为我现在看起来，这个 execute two call 跟在 engine 里面的 use two 方法，好像合并成一个方法的话也比较艰难。这里面有很多细节不太一样，比如它看起来传进了一个 channel

------------------