1. 补一个 pc gateway restart 方法，等价于 pc gateway stop，然后pc gateway start。目标是启动新编译出来的那个版本
2.  SystemPromptBuilder没有在实际项目里使用，只是走了单元测试
3. agents.md的加载和处理过于复杂了，我只是需要一个把文件加载进来作为system prompt开头的一部分就可以了，不需要设定agents.md的文件格式。另外，这个agents.md的文件名，应该是可以在config.yml中设置的
