

1. 调整Engine到Memory的实现逻辑：目前各自在处理sessionId到具体对象的影射关系，其实你应该抽象出一层Session对象，只在Engine中按sessionId管理，接下来message的写入和读取，memory的管理等逻辑都应该在Session对象中实现，而不再需要时刻考虑与sesssionId的影射关系
2. SkillManager应该单独是一个skill_manager.go的文件
3. 扫描并清理代码中的 // TODO待定任务
4. 删除临时生成的脚本
5. 扫描并清理这样的代码：

   ```
   if l := logger.Get(); l != nil {
   		l.Info("Agent state restored")
   	}
   	
   直接改为logo.Info("Agent state restored")
   ```

   