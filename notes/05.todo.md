# 临时想法收集器

###
// SetLLMCallback sets a custom LLM callback for testing.
func (my *Engine) SetLLMCallback(cb llmCallback) {
	my.llmCallback = cb
} 

`setLLMCallback` 这个方法应该已经完整移除了，现在它还有残留，请移除相关的代码，对，移除它们

### 
在进行项目开发时，加一个强要求要求每一个单独的方法不应该超过一屏 也就是单个方法的行数不应该超过 30 行，这是一个强要求。如果某些代码不符合这个要求，就应该进行方法提取和重构把这个需求加入到宪法文件中，也就是 00 constitution.md 文档里面去

